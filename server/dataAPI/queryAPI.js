const { rewriteURIForGET } = require("@apollo/client");

class QueryAPI {

    getTopTen(id) {
    }

    getVideoByID(id) {
    }
    printArgs(args) {
        return args.word
    }
}

module.exports = QueryAPI;